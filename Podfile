target "CleverSDK" do
  platform :ios, "8.0"
  inhibit_all_warnings!
  podspec

  target "CleverSDKTests" do
    inherit! :search_paths

    pod "Specta", "~> 1.0"
    pod "Expecta", "~> 1.0"
  end

  target "Example" do
    inherit! :search_paths
  end
end
